文件夹《计算结果》说明：

文件
DrivableArea.csv
DSF.csv
RiskLevel.csv
中，每一行代表一个具体场景，每一行的每一个值代表场景中某一帧的相应指标的计算结果

文件minResult.csv中，每一行代表一个具体场景，每一行的值代表某个指标在整个场景过程中的极值，从左到右：
最小可行域面积
最小RL
最大DSF

----------------------------------------------------------------------------------------------------------------------------------

文件夹《真值数据》说明：

每一个文件代表一个具体场景，每一行代表一帧，自车/背景车各有9个输出信息，每一行的前9个为自车的信息，其他为背景车
每一行代表的具体信息为：

自车x坐标	自车y坐标	自车x速度	自车y速度	自车纵向加速度	自车相对于道路的朝向	自车绝对朝向(Y轴)	参考路径所在的车道数	自车相对于参考路径的偏移量

背景车x坐标	背景车y坐标	背景车长度	背景车宽度	背景车x速度	背景车y速度	背景车x加速度	背景车y加速度	背景车绝对朝向(Y轴)